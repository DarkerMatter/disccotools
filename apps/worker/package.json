{
  "name": "@disccotools/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "hono": "^4.6.10",
    "@disccotools/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.98.0",
    "@cloudflare/workers-types": "^4.20260603.0",
    "@cloudflare/vitest-pool-workers": "^0.16.13"
  }
}
